<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HeriStepAI.Mobile.Views.POIDetailPage"
             xmlns:viewmodel="clr-namespace:HeriStepAI.Mobile.ViewModels"
             x:DataType="viewmodel:POIDetailViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource PageBackground}">

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Header with POI Image -->
        <Grid Grid.Row="0" HeightRequest="250">
            <Image Source="{Binding SelectedPoi.ImageUrl}"
                   Aspect="AspectFill" />
            <BoxView Color="{StaticResource Black}" Opacity="0.4" />

            <!-- Header Overlay -->
            <Grid RowDefinitions="Auto,*" Padding="20,50,20,20">
                <!-- Back Button -->
                <Frame Grid.Row="0"
                       BackgroundColor="{StaticResource White}"
                       CornerRadius="20"
                       WidthRequest="40"
                       HeightRequest="40"
                       Padding="0"
                       HasShadow="False"
                       HorizontalOptions="Start">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoBackCommand}" />
                    </Frame.GestureRecognizers>
                    <Label Text="←"
                           FontSize="20"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
                </Frame>

                <!-- POI Name and Rating -->
                <VerticalStackLayout Grid.Row="1" VerticalOptions="End" Spacing="8">
                    <Label Text="{Binding SelectedPoi.Name}"
                           TextColor="{StaticResource White}"
                           FontSize="24"
                           FontAttributes="Bold" />

                    <HorizontalStackLayout Spacing="12">
                        <HorizontalStackLayout Spacing="4">
                            <Label Text="⭐" FontSize="16" />
                            <Label Text="{Binding SelectedPoi.RatingText}"
                                   TextColor="{StaticResource White}"
                                   FontSize="15"
                                   FontAttributes="Bold" />
                        </HorizontalStackLayout>

                        <Frame BackgroundColor="{StaticResource Primary}"
                               CornerRadius="8"
                               Padding="8,4"
                               HasShadow="False">
                            <Label Text="{Binding SelectedPoi.CategoryText}"
                                   TextColor="{StaticResource White}"
                                   FontSize="13" />
                        </Frame>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Grid>
        </Grid>

        <!-- Content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="20,20,20,80" Spacing="16">

                <!-- Address -->
                <Frame Style="{StaticResource CardFrame}" Padding="16">
                    <HorizontalStackLayout Spacing="12">
                        <Label Text="📍"
                               FontSize="20"
                               VerticalOptions="Start" />
                        <VerticalStackLayout Spacing="4" HorizontalOptions="FillAndExpand">
                            <Label Text="Địa chỉ"
                                   Style="{StaticResource TitleLabel}"
                                   FontSize="14" />
                            <Label Text="{Binding SelectedPoi.Address}"
                                   Style="{StaticResource BodyLabel}"
                                   LineBreakMode="WordWrap" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Frame>

                <!-- Food Type and Price -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <Frame Style="{StaticResource CardFrame}" Padding="16">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="🍽️" FontSize="24" HorizontalOptions="Center" />
                            <Label Text="Loại món"
                                   Style="{StaticResource CaptionLabel}"
                                   HorizontalOptions="Center" />
                            <Label Text="{Binding SelectedPoi.FoodTypeText}"
                                   Style="{StaticResource TitleLabel}"
                                   FontSize="13"
                                   HorizontalOptions="Center"
                                   LineBreakMode="WordWrap"
                                   MaxLines="2" />
                        </VerticalStackLayout>
                    </Frame>

                    <Frame Grid.Column="1" Style="{StaticResource CardFrame}" Padding="16">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="💰" FontSize="24" HorizontalOptions="Center" />
                            <Label Text="Giá"
                                   Style="{StaticResource CaptionLabel}"
                                   HorizontalOptions="Center" />
                            <Label Text="{Binding SelectedPoi.PriceRangeText}"
                                   Style="{StaticResource TitleLabel}"
                                   FontSize="13"
                                   TextColor="{StaticResource Secondary}"
                                   HorizontalOptions="Center"
                                   LineBreakMode="WordWrap"
                                   MaxLines="2" />
                        </VerticalStackLayout>
                    </Frame>
                </Grid>

                <!-- Visit Time -->
                <Frame Style="{StaticResource CardFrame}" Padding="16">
                    <HorizontalStackLayout Spacing="12">
                        <Label Text="⏱"
                               FontSize="20"
                               VerticalOptions="Center" />
                        <VerticalStackLayout Spacing="4">
                            <Label Text="Thời gian tham quan"
                                   Style="{StaticResource TitleLabel}"
                                   FontSize="14" />
                            <Label Text="{Binding SelectedPoi.EstimatedMinutes, StringFormat='{0} phút'}"
                                   Style="{StaticResource BodyLabel}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Frame>

                <!-- Description -->
                <Frame Style="{StaticResource CardFrame}" Padding="16">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Thông tin"
                               Style="{StaticResource TitleLabel}"
                               FontSize="16" />
                        <Label Text="{Binding SelectedPoi.Description}"
                               Style="{StaticResource BodyLabel}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom Action Bar -->
        <Frame Grid.Row="2"
               BackgroundColor="{StaticResource White}"
               CornerRadius="0"
               Padding="16,12"
               HasShadow="True">
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button Grid.Column="0"
                        Text="🔊 Nghe thuyết minh"
                        Command="{Binding PlayNarrationCommand}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="{StaticResource White}"
                        CornerRadius="25"
                        HeightRequest="50" />
                <Button Grid.Column="1"
                        Text="🗺️ Chỉ đường"
                        Command="{Binding NavigateCommand}"
                        BackgroundColor="{StaticResource White}"
                        TextColor="{StaticResource Primary}"
                        BorderColor="{StaticResource Primary}"
                        BorderWidth="2"
                        CornerRadius="25"
                        HeightRequest="50" />
            </Grid>
        </Frame>
    </Grid>

</ContentPage>
